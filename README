------------------Installation Procedure ---------------------------------------

1. Install and Run redis server
    Ensure the redis server is running at 127.0.0.1:6379 (link to download ans install redis https://redis.io/download)

2. Clone the repository from Github
3. Open three terminals
4. Cd into the directory where the cloned files are present
5. Initiate the racer1 by using the command python3 racer1.py in terminal 1
6. Initiate the racer2 by using the command python3 racer2.py in terminal 2
7. Initiate the master by using the command python3 master.py in terminal 3

----------------------------------------------------------------------------------
dependencies requirement --
python 3.8
redis latest release
install the following module if not present redis, datetime, random
(link to install python redis module)

-----------------------------------------------------------------------------------